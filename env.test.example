# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test Database Configuration (if needed later)
# TEST_DB_HOST=localhost
# TEST_DB_PORT=5432
# TEST_DB_NAME=express_api_test
# TEST_DB_USER=postgres
# TEST_DB_PASSWORD=password

# Test JWT Configuration (if needed later)
# TEST_JWT_SECRET=test-jwt-secret-key
# TEST_JWT_EXPIRES_IN=1h

# Test API Configuration
API_VERSION=v1
API_PREFIX=/api

# Test Logging
LOG_LEVEL=error

# Test CORS Configuration
CORS_ORIGIN=http://localhost:3001

# Test Rate Limiting (more permissive for tests)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000 